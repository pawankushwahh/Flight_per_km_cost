#  Flight Cost Analysis Web App (Frontend)

This is the frontend interface for the **Flight Cost Analysis** project — a full-stack application that helps users compare flight prices, calculate cost-per-km, find affordable routes, and view insightful travel visualizations.

The frontend interacts with the backend APIs to display:
✔ Cost-per-km flight comparisons  
✔ Route-based price predictions  
✔ Nearby airport suggestions  
✔ Class & layover information  
✔ Heatmap-based pricing data  
✔ Airport lists and search utilities  

---

##  Features (Frontend)

- Search and compare flight costs by route
- Display trend insights returned from backend predictions
- Visualize flight affordability using heatmap data
- Auto-suggest airports using backend airports API
- Clean and responsive UI for smooth experience

---

##  Tech Stack

| Category | Technology |
|---------|------------|
| Language | JavaScript |
| Frontend | HTML, CSS, JavaScript (Vanilla) |
| Communication | REST APIs (JSON) |
| Deployment | Static Hosting / Compatible with Render / Vercel |

---

##  Backend Repository

For API details and data processing:
👉 **Backend:** https://github.com/pawankushwahh/Flight_per_km_backend

Both repositories together complete the full-stack project.

---

## 📡 API Endpoints Used

- `/api/compare` → Compare routes by price and cost-per-km  
- `/api/predict` → Price trend prediction for selected route  
- `/api/nearby-airports` → Suggest nearest airports  
- `/api/class-layover` → Class and layover details  
- `/api/heatmap` → Heatmap visualization data  
- `/api/airports` → List of unique airports  
- `/api/raw-compare-data` → Complete dataset lookup  

Backend returns formatted JSON for frontend consumption.

